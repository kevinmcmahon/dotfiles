# Book Clean Architecture

Use $book-clean-architecture as the primary lens for this task.

Before editing, briefly state:

- What is the policy core, and where should it live?
- How do dependencies currently violate the dependency rule?
- Which layer boundaries and ports/adapters are changing in this patch?

Then proceed with the requested work unless essential context is missing. Keep the book lens subordinate to the user's instructions, repository instructions, and the project's architecture.
