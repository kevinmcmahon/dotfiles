# Book Legacy Change

Use $book-legacy-change as the primary lens for this task.

Before editing, briefly state:

- the requested behavior change
- the existing behavior that must be protected
- the legacy-code risk, change point, and blocking dependencies
- the characterization, observation point, or tests that will prove the result

Then proceed with the requested work unless essential context is missing. Keep the book lens subordinate to the user's instructions, repository instructions, and the project's existing architecture. Prefer the smallest useful seam and avoid unrelated cleanup.
